# PDFs for full-text extraction

Place inaccessible papers here as `{DOI_sanitized}.pdf`
e.g., `10.1016_j.hal.2022.102287.pdf` (replace `/` with `_`)

Claude Code will check this directory as a fallback when Unpaywall returns no open access URL.
